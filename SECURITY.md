# Safety and Security

A mobile robot can move unexpectedly when localization, transforms, or velocity-control software is misconfigured. Validate TF and sensor data with drive motors disabled or the platform safely supported before autonomous motion.

Do not commit Wi-Fi passwords, API keys, SSH keys, robot credentials, or private network details.
