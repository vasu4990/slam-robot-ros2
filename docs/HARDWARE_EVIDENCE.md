# Physical Hardware Evidence

Simulation evidence must never be relabeled as hardware evidence. A hardware validation bundle should contain LiDAR model/firmware, encoder resolution, wheel geometry/calibration, timestamp source, `/scan` and `/odom` rates, TF audit, rosbag, map artifacts, repeatability runs, thermal/power notes and failure observations.

The validation flags stay false until those artifacts exist.
